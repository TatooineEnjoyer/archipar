ARCHIPAR — финальная статическая версия

Сделано:
1. Удалены poster-изображения и стартовые заглушки из всех hero-секций.
2. Все пять hero-видео физически собраны в формате «вперёд → назад» и зациклены без резкого старта сначала.
3. Белый/пустой ролик банных комплексов заменён на рабочее видео из исходного архива.
4. Исправлены пути галерей саун и хамамов: теперь используются реально существующие изображения из public/images.
5. Проект переведён на статический экспорт Next.js без SSR: результат сборки — папка out.
6. Удалён серверный API /api/leads и зависимость Supabase.
7. Форма поддерживает внешний NEXT_PUBLIC_LEADS_ENDPOINT, но без переменной не ломает сборку.
8. Header упрощён: сворачивается при прокрутке вниз, раскрывается при прокрутке вверх или клике.
9. Стекло header стало серо-бежевым (taupe), переход сделан медленнее, верхний зазор отсутствует.

Timeweb Cloud:
- Тип: Frontend
- SSR: выключен
- Команда сборки: npm run build
- Директория сборки: out
- Node.js: 22 или 24

Важно:
- Папку public не удалять.
- Видео уже обработаны в ping-pong; повторная обработка не требуется.
- Для реальной отправки заявок нужен внешний POST endpoint в NEXT_PUBLIC_LEADS_ENDPOINT.
